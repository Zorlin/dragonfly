// Remove unused imports
// use std::process::Command;

// Remove unused import
// use http_body_util::StreamBody;
// Remove unused import
// use crate::InstallationState; // This is used, keep it

// Remove unused imports from db.rs
// Remove: DiskInfo, HostnameUpdateRequest, HostnameUpdateResponse, BmcCredentialsUpdateRequest, OsInstalledUpdateRequest, OsInstalledUpdateResponse, StatusUpdateRequest
// Keep: MachineStatus, BmcType, BmcCredentials, OsAssignmentRequest, RegisterResponse, ErrorResponse, InstallationProgressUpdateRequest (if used)
// Adjust the re-export line accordingly
pub use dragonfly_common::models::{BmcCredentials, BmcType, OsAssignmentRequest, RegisterResponse, ErrorResponse, InstallationProgressUpdateRequest, OsInstalledUpdateResponse, StatusUpdateRequest, HostnameUpdateResponse}; // Adjust based on actual usage

// Remove unused imports from filters.rs
// Remove: DateTime, Utc

// Remove unused import from ui.rs
// use minijinja::Environment;
// Remove unused import from ui.rs
// use crate::InstallationState; // This is used, keep it

// Remove unused imports from mode.rs
// use std::os::unix::fs::PermissionsExt;
// use nix::libc;

// Fix unused variable in ui.rs
// Change `State(app_state): State<crate::AppState>` to `State(_app_state): State<crate::AppState>` in `settings_page` if `app_state` isn't actually used

// Fix unused mut in mode.rs
// Remove `mut` from `let mut used_elevation = false;` if it's never reassigned 